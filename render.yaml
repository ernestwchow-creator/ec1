services:
  - type: web
    name: oxacheck
    runtime: node
    plan: free
    rootDir: app
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
